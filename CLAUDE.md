# 작업 규칙

Keras 딥러닝 학습용 저장소. 수업을 따라가며 실습 코드를 쓰고, 그날 배운 개념을 노트로 남긴다.

## 구조

```
keras/      실습 스크립트. 파일명은 keras<번호>_<주제>.py 로 수업 진도를 따라감
note/       dayNN_MMDD_keras.ipynb 학습 노트 + README.md 색인
note/assets/ 노트에 들어가는 그림
_data/      원본 데이터셋 (.gitignore)
_save/      모델·가중치·npy (.gitignore)
```

## 환경

conda 환경 `tf29x-gpu` (TensorFlow 2.9). `python`만 치면 Windows 스토어 스텁이 잡혀 `exit 49`로 끝나므로, 활성화를 먼저 확인한다.

```
conda activate tf29x-gpu
```

## 학습 노트

형식은 `note/README.md`의 **작성 규칙**을 따른다. 그 외에 지킬 것은 아래.

- 제목은 **최대한 간결한 명사**로. 대단원은 용어 하나 수준 (`Flatten`, `padding`, `경사하강법`, `데이터 증폭`)
- 줄 끝에 마침표를 쓰지 않는다
- `주의)` `cf)` `예외)`는 번호를 붙이지 않고 `####`로 단다. 위치는 해당 대단원 맨 뒤
- 어조는 음/함 체. 과장·의인화 표현(`태워서`, `뭉개짐`, `거짓말을 함`)은 쓰지 않고 수치와 동작으로 적는다
- **그날 배운 것만** 쓴다. 따로 돌려본 검증이나 분석을 섹션으로 덧붙이지 않는다
- 코드는 개념만. 문법 자체가 개념인 경우(`x[[3, 7]]` 같은) 2~5줄은 넣어도 되고, 실행 가능한 전체 스크립트·`print` 출력·에러 사례는 해당 `.py` 주석으로 보낸다
- 굵은 글씨는 규칙 자체를 말하는 자리에만. 불릿마다 붙이지 않는다

대단원 번호를 바꾸면 `note/README.md`의 `dayNN §N` 참조도 같이 고친다.

## 참조 문서

여러 날에 걸친 내용을 묶은 문서는 `note/`에 따로 둔다. 학습 노트와 달리 **코드를 넣는다** — 작업하면서 옆에 놓고 보는 용도라 이론과 코드가 붙어 있어야 한다.

`note/cnn_tuning.md`는 로컬 전용이라 `.gitignore`에 있다.

## 실습 코드

- 실행 결과는 기대값을 주석으로 적는다 (`# acc : 0.9174`)
- 값이 바뀌면 `이전 > 이후` 형태로 남긴다 (`# 0.9081 > 0.9174`)
- 코드를 고칠 때 이유가 필요하면 그 줄 옆에 한 줄 주석으로

## 커밋

```
Docs: <영문 한 줄 요약>

<한글 본문. 무엇을 왜 했는지>
```

날짜(`day17`)와 그날의 대표 개념을 같이 적는다.
